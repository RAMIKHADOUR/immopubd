{% extends 'users.html.twig' %}

{% block title %} Modification du Profil de {{app.user.prenom}} {{app.user.nom}}{% endblock %}

{% block body %}

    <!-- Code here the main content -->

    <div class="grix xs4 container m-4">
        <div class="col-xs4 m-4">
            <h1> Modification du Profil de :{{app.user.prenom}} {{app.user.nom}}</h1>
            {{form(form)}}
        </div>
        <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
            <button type="submit" data-mdb-button-init data-mdb-ripple-init class="btn btn-lg fw-bold"
                style="background-color:rgb(14, 205, 14);">Edit</button>
        </div>
    </div>  



{% endblock %}