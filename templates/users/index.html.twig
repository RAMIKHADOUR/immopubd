{% extends 'users.html.twig' %}

{% block title %}Profil de {{app.user.prenom}} {{app.user.nom}}{% endblock %}

{% block body %}
<main>
    <!-- Code here the main content -->

    <div class="grix xs4 container m-4">
        <div class="col-xs4 m-4">
            <h1> Profile de :{{app.user.prenom}} {{app.user.nom}}</h1>
        </div>
        <div>Nom : </div>
        <div>{{app.user.nom}}</div>
        <div>| Prenom :</div>
        <div>{{app.user.prenom}}</div>
        <div>E-mail :</div>
        <div>{{app.user.email}}</div>

        <div class="col-xs4 m-4">
            <a href="{{path('user_edit_profile')}}" class="btn shadow-1 rounded-1 blue">Modifier le Profile</a>
            <a href="" class="btn shadow-1 rounded-1 blue">Modifier le mot de passe</a>
            <a href="{{path('app_annonces_new')}}" class="btn shadow-1 rounded-1 blue">Ajoute Une Annonce</a>
        </div>

        {# <div class="col-xs4 m-4">
            <h4> Annonces de : {{app.user.prenom}} {{app.user.nom}} ({{ app.user.propertys|length }})</h4>
        </div>

        {% for property in app.user.propertys %}
        <div> | Reference :</div>
        <div>{{property.reference}}</div>
        {% endfor %}
    </div> #}

</main>

{% endblock %}