{% extends 'base.html.twig' %}

{% block title %}Categorysbien{% endblock %}

{% block body %}
    <h1>Categorysbien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorysbien.id }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ categorysbien.categorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorysbien_index') }}">back to list</a>

    <a href="{{ path('app_categorysbien_edit', {'id': categorysbien.id}) }}">edit</a>

    {{ include('categorysbien/_delete_form.html.twig') }}
{% endblock %}
