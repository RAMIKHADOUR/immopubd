{% extends 'base.html.twig' %}

{% block title %}Categorysbien index{% endblock %}

{% block body %}
    <h1>Categorysbien index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorysbien in categorysbiens %}
            <tr>
                <td>{{ categorysbien.id }}</td>
                <td>{{ categorysbien.categorie }}</td>
                <td>
                    <a href="{{ path('app_categorysbien_show', {'id': categorysbien.id}) }}">show</a>
                    <a href="{{ path('app_categorysbien_edit', {'id': categorysbien.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorysbien_new') }}">Create new</a>
{% endblock %}
