{% extends 'base.html.twig' %}

{% block title %}Typesbien{% endblock %}

{% block body %}
    <h1>Typesbien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typesbien.id }}</td>
            </tr>
            <tr>
                <th>Type_bien</th>
                <td>{{ typesbien.typeBien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typesbien_index') }}">back to list</a>

    <a href="{{ path('app_typesbien_edit', {'id': typesbien.id}) }}">edit</a>

    {{ include('typesbien/_delete_form.html.twig') }}
{% endblock %}
